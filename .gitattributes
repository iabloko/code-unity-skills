# Normalize line endings; bash scripts and git hooks must stay LF
# even on Windows checkouts (CRLF breaks `#!/bin/bash` scripts).
* text=auto
*.sh text eol=lf
skills/committing-changes/scripts/** text eol=lf